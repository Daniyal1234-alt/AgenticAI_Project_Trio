# --- Agent / LLM stack ---
langgraph>=0.2.50
langchain-core>=0.3.0
langchain-openai>=0.2.0
openai>=1.40.0
pydantic>=2.7.0

# --- Phase 1 / shared ---
python-dotenv>=1.0.0

# --- Phase 2 (audio) ---
edge-tts>=7.0.0

# --- Phase 3 (video) ---
moviepy>=2.1.0          # works with v1.0.x too — pipeline picks the right API
Pillow>=10.0.0
imageio-ffmpeg>=0.5.0   # bundles a portable ffmpeg for moviepy

# Phase 3 — local Stable Diffusion (CPU). All optional at runtime: the pipeline
# falls back to DALL-E (if OPENAI_API_KEY is set) and then PIL placeholder.
# For CPU-only torch install:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
diffusers>=0.27.0
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.0

# Phase 3 — audio resampling for lip-sync (MP3 → 16kHz WAV).
librosa>=0.10.0
soundfile>=0.12.0

# --- Phase 4 (web) ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0           # used by FastAPI's TestClient

# --- Tests ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
